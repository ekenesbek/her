"""STT adapter shims. The default Deepgram adapter is wired in voice_agent.main.

Add alternate STT engines here (e.g. Whisper streaming, AssemblyAI) by exposing
something compatible with `livekit.agents.stt.STT`.
"""
